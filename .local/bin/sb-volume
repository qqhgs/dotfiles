#! /usr/bin/env bash

[ $(amixer get Master | awk 'END{print $6}') = "[off]" ] && echo " " && exit

vol="$(amixer get Master | awk 'END{print substr($4, 2)}' | awk '{split($1, a, "%"); print a[1]}')"
icon=" "

echo "$icon$vol"
